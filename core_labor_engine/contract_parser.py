# ============================================================
# 📄 الملف: contract_parser.py
# 📄 قراءة وتحليل ملفات العقود PDF مؤقتًا قبل التكامل مع قوى
# ============================================================

import fitz  # مكتبة PyMuPDF

def extract_contract_data(pdf_path):
    """قراءة محتوى العقد من ملف PDF واستخراج البيانات النصية."""
    data = {}
    with fitz.open(pdf_path) as doc:
        text = "".join([page.get_text() for page in doc])
        data["text"] = text
    return data
